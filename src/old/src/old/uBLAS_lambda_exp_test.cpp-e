/*
 * =====================================================================================
 *
 *       Filename:  matrixtest.cpp
 *
 *    Description:
 *
 *        Version:  1.0
 *        Created:  05/26/2014 12:46:33 PM
 *       Revision:  none
 *       Compiler:  gcc
 *
 *         Author:  Anup Pillai (), anupgpillai@gmail.com
 *   Organization:  IISER Pune
 *
 * =====================================================================================
 */

#include <iostream>
#include <vector>
//#include "/opt/boost/boost_1_55_0/boost/numeric/ublas/vector.hpp" /* Specifying the full path in qoutes for a header file */
//#include <boost/numeric/ublas/matrix.hpp> /* Specifying only the path from root directory: must have an -I option when calling compiler when used this way example: g++ -I /opt/boost/boost_1_55_0 matrixtest.cpp -o matrixtest */
#include <boost/numeric/ublas/io.hpp>
using namespace std;
using namespace boost::numeric::ublas;
using namespace boost::numeric::odeint;

main()
{
   // Create a uBLAS matrix using boost
   //matrix<double > myMat (3,3,2.5);
   v
   string prompt("What is your name: "), name;
   cout << prompt;
   getline(cin, name);
   cout << "Your name is: " << name << endl;
   cout << "Your name is " << name.length() << " characters long" << endl;
   // Create a uBLAS vector using boost
   boost::numeric::ublas::vector<double > myVec (4,45);
   std::cout << myMat << "\n";
   std::cout << myVec << "\n";
   std::cout << myMat(2,2) << "\n";
   /* Create a lambda function : inline functions in c++ */
   auto mylambda = [] (double a, double b) {double c = a*b; return (c);};
   cout << mylambda(4,3) << endl;
   std::cout << "Current version of c++: " <<  __cplusplus << "\n";
}
